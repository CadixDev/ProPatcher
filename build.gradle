plugins {
    id 'groovy'

    id 'maven'
    id 'com.gradle.plugin-publish' version '0.9.1'

    id 'eclipse'
    id 'idea'

    id 'com.github.hierynomus.license' version '0.11.0'
}

sourceCompatibility = 1.8
targetCompatibility = 1.8

group = 'uk.jamierocks'
version = '1.2-SNAPSHOT'
description = 'ProPatcher is a Gradle plugin for creating .patch files on the go.'

repositories {
    mavenLocal()
    mavenCentral()
}

dependencies {
    compile gradleApi()
    compile localGroovy()

    compile 'com.cloudbees:diff4j:1.2'
    compile 'org.apache.commons:commons-io:1.3.2'
}

license {
    header file('HEADER.txt')
    include '**/*.java'
    include '**/*.groovy'

    strictCheck true
    mapping {
        java = 'SLASHSTAR_STYLE'
    }
}

pluginBundle {
    website = 'https://github.com/jamierocks/propatcher'
    vcsUrl = 'https://github.com/jamierocks/propatcher'
    description = project.description
    tags = ['patches']

    plugins {
        propatcherPlugin {
            id = 'uk.jamierocks.propatcher'
            displayName = 'propatcher'
        }
    }
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.7'
}